# Code Generated by Sidekick is for learning and experimentation purposes only.
# Local Terraform working directory
.terraform/
**/.terraform/*

# Terraform state files (never commit)
*.tfstate
*.tfstate.*

# Terraform crash logs
crash.log
crash.*.log

# CLI config files (often contain creds/tokens)
.terraformrc
terraform.rc

# Override files (local-only)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Variable files (often contain secrets; commit only sanitized examples)
*.tfvars
*.tfvars.json

# Terraform lock info (optional: usually you DO commit this)
# .terraform.lock.hcl

